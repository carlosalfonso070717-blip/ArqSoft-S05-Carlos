name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configurar .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restaurar
        run: dotnet restore Citas_App.slnx

      - name: Compilar
        run: dotnet build Citas_App.slnx --no-restore

      - name: Correr pruebas
        run: dotnet test Citas_App.slnx --no-build --verbosity normal
